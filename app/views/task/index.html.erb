<h1>Tasks</h1>

<% @tasks.each do |task| %>
  <p><%= task.title %></p>
<% end %>

<%= form_with model: @task, url: tasks_path, local: true do |form| %>
  <%= form.text_field :title, placeholder: "Add your Quest" %>
  <%= form.submit "Submit" %>
<% end %>
